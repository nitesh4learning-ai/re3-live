import WorkPage from '../../components/pages/WorkPage';

export const metadata = {
  title: 'Work — Re³',
  description: 'A piece of work from the Re³ studio.',
};

export default function WorkRoute() {
  return <WorkPage />;
}
